#!/usr/bin/env bash
set -eou pipefail

_apk(){
  apk add -U postfix mutt
}

_vim(){
  git clone https://github.com/Shougo/neobundle.vim ~/.vim/bundle \
    && ~/.vim/bundle/bin/install.sh
}

_pip(){
  pip install --upgrade awscli
}

_apk & 2>/dev/null; _vim & 2>/dev/null; _pip & 2>/dev/null;
wait
